print "Iniciando prueba de enums";

enum Result {
    Ok(valor),
    Err(mensaje)
}

print "Enum definido";

let resultado1 = Ok(42);

print "Instancia creada";

match resultado1 {
    Ok(v) => print v,
    Err(e) => print e
}

print "Match terminado";
