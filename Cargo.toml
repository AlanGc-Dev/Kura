[package]
name = "Kura"
version = "0.1.0"
edition = "2024"

[dependencies]
# Para descargar librerías de internet
reqwest = { version = "0.11", features = ["blocking"] }
# Para entender y escribir el archivo kura.toml
serde = { version = "1.0", features = ["derive"] }
toml = "0.5"